{% extends "::layout/layout.html.twig"%}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <h1>{{ page.titre }}</h1>
            <div class="col-lg-12">
                {{ page.contenu |raw }}
            </div>
        </div>
    </div>
{% endblock %}