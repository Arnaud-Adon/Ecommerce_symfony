{% extends '::layout/layout.admin.html.twig' %}

{% block body %}
    <h1>Création d'une catégorie</h1>

    <div class="col-lg-12">
        <div class="col-lg-12">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-success" type="submit" value="Créer la catégorie" /><br>
            {{ form_end(form) }}
        </div>
        
        <div class="col-lg-12">
            <button class="btn btn-default"><a href="{{ path('adminCategories_index') }}" style="color:#000;text-decoration: none;">Retour à la liste des catégories</a></button>
        </div>
    </div>
{% endblock %}
